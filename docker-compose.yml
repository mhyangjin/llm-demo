version: '3.8'

services:
  semantic-api:
    build: .
    container_name: semantic-api
    ports:
      - "8091:8080"
    environment:
      - APP_HOST=0.0.0.0
      - APP_PORT=8080
    restart: unless-stopped

